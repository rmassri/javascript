const name = "Richard";
const real = "Jose";

const normal = name + " " + real;

const template = `${1 + 1} ${name} ${real}`;

console.log(template);

const html = `
<h1> Hola </h1>
<p> Mundo </p>
`;

console.log(html);
